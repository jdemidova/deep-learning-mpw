# This project is managed with uv.
# Export a pinned requirements file when needed with:
# uv export --no-dev --format requirements-txt -o requirements.txt

numpy
pandas
matplotlib
Pillow
torch>=2.6
torchvision>=0.21
tqdm
nltk
ipykernel
jupyterlab